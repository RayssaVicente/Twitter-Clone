<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>X / Home</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/timeline.css">
</head>
<body class="bg-black text-white">

<div class="container-fluid">
  <div class="row min-vh-100">
  
    
    <div class="col-2 d-flex flex-column justify-content-between pt-3 ps-3 border-end border-secondary order-1">
      <div>
        <div class="mb-4">
          <img src="/img/logo-twwiter.avif" alt="Logo X" style="width: 40px;">
        </div>
        <ul class="nav flex-column">
          <li class="nav-item mb-2"><a class="nav-link text-white" href="#"><img src="/img/house-solid-full.png" alt="" style="width: 30px;"> Home</a></li>
          <li class="nav-item mb-2"><a class="nav-link text-white" href="#"> <img src="/img/magnifying-glass-solid-full.png" alt="" style="width: 30px;">Explore</a></li>
          <li class="nav-item mb-2"><a class="nav-link text-white" href="#"><img src="/img/bell-regular-full.png" alt="" style="width: 30px;"> Notifications</a></li>
          <li class="nav-item mb-2"><a class="nav-link text-white" href="#"><img src="/img/envelope-solid-full.png" alt="" style="width: 30px;"> Messages</a></li>
          <li class="nav-item mb-2"><a class="nav-link text-white" href="#"><img src="/img/fork-removebg-preview.png" alt="" style="width: 30px;"> Grok</a></li>
          <li class="nav-item mb-2"><a class="nav-link text-white" href="/perfil"><img src="/img/user-solid-full.png" alt="" style="width: 30px;"> Profile</a></li>
          <li class="nav-item mb-2"><a class="nav-link text-white" href="#"><img src="/img/more.png" alt="" style="width: 40px;"> More</a></li>
        </ul>
        <button class="btn btn-light text-dark w-100 rounded-pill fw-bold">Post</button>
      </div>
      <div class="pb-3 small d-flex align-items-center">
        <div class="fw-bold me-2"><?= htmlspecialchars($_SESSION['nome']) ?></div>
        <div class="text-muted">@<?= htmlspecialchars($_SESSION['nome']) ?></div>
      </div>
    </div>

   
    <!-- <div class="col-3 py-3 px-4 border-start border-secondary order-3">
      <div class="p-3 border-bottom border-secondary">
        <form method="GET" action="/timeline" class="d-flex">
          <input 
            type="text" 
            name="pesquisarPor" 
            class="form-control me-2 bg-black text-white border border-secondary" 
            placeholder="Quem você está procurando?" 
            autocomplete="off"
            value="<?= isset($_GET['pesquisarPor']) ? htmlspecialchars($_GET['pesquisarPor']) : '' ?>">
          <button type="submit" class="btn btn-outline-light btn-sm rounded-pill">Procurar</button>
        </form>
      </div>
      
      <?php if (!empty($this->view->usuarios)) : ?>
        <div class="px-3">
          <?php foreach($this->view->usuarios as $usuario): ?>
            <div class="d-flex justify-content-between align-items-center py-2 border-bottom border-secondary">
              <div>
                <div class="fw-bold"><?= htmlspecialchars($usuario['nome']) ?></div>
                <div class="text-muted small">@<?= htmlspecialchars($usuario['nome']) ?></div>
              </div>
              <?php if($usuario['seguindo_sn'] == 0): ?>
                <a href="/acao?acao=seguir&id_usuario=<?= $usuario['id'] ?>" class="btn btn-outline-light btn-sm rounded-pill">Seguir</a>
              <?php else: ?>
                <a href="/acao?acao=deixar_de_seguir&id_usuario=<?= $usuario['id'] ?>" class="btn btn-danger btn-sm rounded-pill">Deixar de seguir</a>
              <?php endif; ?>
            </div>
          <?php endforeach; ?>
        </div>
      <?php elseif (isset($_GET['pesquisarPor'])): ?>
        <div class="px-3 text-muted">Nenhum usuário encontrado.</div>
      <?php endif; ?> -->
        <div class="col-3 py-3 px-4 border-start border-secondary order-3">
  <div class="p-3 border-bottom border-secondary mb-3">
    <form method="GET" action="/timeline" class="d-flex">
      <input 
        type="text" 
        name="pesquisarPor" 
        class="form-control me-2 bg-black text-white border border-secondary rounded-pill ps-3" 
        placeholder="Search users..." 
        autocomplete="off">
      <button type="submit" class="btn btn-outline-light btn-sm rounded-pill">Search</button>
    </form>
  </div>
  
  <h5 class="fw-bold px-3 mb-3">Who to follow</h5>

  <?php if (!empty($this->view->usuarios)) : ?>
    <div class="px-2">
      <?php foreach($this->view->usuarios as $usuario): ?>
        <div class="d-flex justify-content-between align-items-center py-3 px-2 border-bottom border-secondary">
          <div class="d-flex align-items-center">
             <div class="rounded-circle bg-secondary me-2 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                <?= strtoupper(substr($usuario['nome'], 0, 1)) ?>
             </div>
             <div>
                <div class="fw-bold small"><?= htmlspecialchars($usuario['nome']) ?></div>
                <div class="text-muted small">@<?= htmlspecialchars($usuario['nome']) ?></div>
             </div>
          </div>
          <?php if($usuario['seguindo_sn'] == 0): ?>
            <a href="/acao?acao=seguir&id_usuario=<?= $usuario['id'] ?>" class="btn btn-light btn-sm rounded-pill fw-bold">Follow</a>
          <?php else: ?>
            <a href="/acao?acao=deixar_de_seguir&id_usuario=<?= $usuario['id'] ?>" class="btn btn-danger btn-sm rounded-pill fw-bold">Unfollow</a>
          <?php endif; ?>
        </div>
      <?php endforeach; ?>
    </div>
  <?php endif; ?>
</div>
      <div class="px-3 py-2">
        <a href="/timeline" class="text-info text-decoration-none small">Procurar por pessoas conhecidas</a>
      </div>
    </div>

    <!-- Feed principal com formulário e tweets -->
    <div class="col-7 border-start border-end border-secondary px-0 order-2">
      <div class="d-flex justify-content-between px-3 py-2 border-bottom border-secondary">
        <div class="fw-bold border-bottom border-primary">For you</div>
        <a href="/perfil">
          <div class="text" style="color: #8899a6;">Following</div>
        </a>
      </div>

      <!-- Formulário de Postagem -->
      <div class="d-flex px-3 py-3 border-bottom border-secondary">
        <div class="me-3">
          <a href="/perfil">
            <div class="bg-warning text-black rounded-circle d-flex align-items-center justify-content-center" style="width: 48px; height: 48px; font-weight: bold; font-size: 1.2rem;">
              <?php 
                $nome = $_SESSION['nome'];
                echo htmlspecialchars(substr($nome, 0, 1));
              ?>
            </div>
          </a>
        </div>
        <div class="flex-grow-1">
          <form action="/tweet" method="post">
            <textarea class="form-control caixa-postagem bg-black border fw-bold" placeholder="What's happening?" rows="2" style="resize: none;" name="tweet"></textarea>
            <div class="d-flex justify-content-between mt-2">
              <div class="d-flex gap-3 text-info">
                <i class="bi bi-image"></i>
                <i class="bi bi-filetype-gif"></i>
                <i class="bi bi-bar-chart-line"></i>
                <i class="bi bi-emoji-smile"></i>
                <i class="bi bi-calendar3"></i>
                <i class="bi bi-geo-alt"></i>
              </div>
              <button class="btn btn-light fw-bold btn-sm rounded-pill px-3">Post</button>
            </div>
          </form>
        </div>
      </div>

      <!-- Lista de Tweets -->
      <?php foreach($this->view->tweets as $id_tweet => $tweet): ?>
        <div class="px-3 pt-3 pb-1 border-bottom border-secondary">
          
          <div class="d-flex align-items-center mb-1">
            <span class="fw-bold me-1"><?= htmlspecialchars($tweet['nome']) ?></span>
            <span class="text" style="color: #8899a6;">@<?= htmlspecialchars($tweet['nome']) ?> · <?= htmlspecialchars($tweet['data']) ?></span>
          </div>

          <div class="mb-2"><?= nl2br(htmlspecialchars($tweet['tweet'])) ?></div>

          <?php if($tweet['id_usuario'] == $_SESSION['id']) {?>
              <form method="post" action="/remover" onsubmit="return confirm('Deseja realmente excluir este tweet?');">
              <input type="hidden" name="tweet_id" value="<?= $tweet['id'] ?>">
              <button type="submit" class="btn btn-danger btn-sm">Excluir</button>
              </form>

          <?php }?>
        </div>
      <?php endforeach ?>
    </div>

  </div>
</div>

</body>
</html>
