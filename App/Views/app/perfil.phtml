<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>X / Perfil</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/timeline.css">
  <style>
    /* Estilos para garantir que as imagens fiquem perfeitas no círculo */
    .object-fit-cover { object-fit: cover; }
    .form-floating > .form-control:focus ~ label,
    .form-floating > .form-control:not(:placeholder-shown) ~ label {
        color: #1d9bf0 !important;
    }
    .form-floating > .form-control:focus {
         border-color: #1d9bf0 !important;
         box-shadow: none;
    }
    .btn-edit-photo {
        background: rgba(0, 0, 0, 0.5);
        transition: 0.3s;
    }
    .btn-edit-photo:hover {
        background: rgba(0, 0, 0, 0.7);
    }
  </style>
</head>
<body class="bg-black text-white">

<div class="container-fluid">
  <div class="row min-vh-100">
  
    <div class="col-2 d-flex flex-column justify-content-between pt-3 ps-3 border-end border-secondary order-1">
      <div>
        <div class="mb-4">
          <a href="/sair" title="Sair do sistema">
            <img src="/img/logo-twwiter.avif" alt="Logo X" style="width: 40px;">
          </a>
        </div>
        <ul class="nav flex-column">
          <li class="nav-item mb-2"><a class="nav-link text-white" href="/timeline"><img src="/img/house-solid-full.png" alt="" style="width: 30px;"> Home</a></li>
          <li class="nav-item mb-2"><a class="nav-link text-white" href="#"> <img src="/img/magnifying-glass-solid-full.png" alt="" style="width: 30px;"> Explore</a></li>
          <li class="nav-item mb-2"><a class="nav-link text-white" href="#"><img src="/img/bell-regular-full.png" alt="" style="width: 30px;"> Notifications</a></li>
          <li class="nav-item mb-2"><a class="nav-link text-white" href="#"><img src="/img/envelope-solid-full.png" alt="" style="width: 30px;"> Messages</a></li>
          <li class="nav-item mb-2"><a class="nav-link text-white" href="#"><img src="/img/fork-removebg-preview.png" alt="" style="width: 30px;"> Grok</a></li>
          <li class="nav-item mb-2"><a class="nav-link text-white" href="#"><img src="/img/user-solid-full.png" alt="" style="width: 30px;"> Profile</a></li>
        </ul>
        <button class="btn btn-light text-dark w-100 rounded-pill fw-bold mt-3">Post</button>
      </div>
      <div class="pb-3 small d-flex align-items-center">
        <div class="fw-bold me-2"><?= htmlspecialchars($_SESSION['nome']) ?></div>
        <div class="text-muted">@<?= htmlspecialchars($_SESSION['nome']) ?></div>
      </div>
    </div>

    <div class="col-7 border-start border-end border-secondary px-0 order-2">
      
      <div class="p-3 d-flex align-items-center border-bottom border-secondary sticky-top bg-black" style="z-index: 1000;">
          <a href="/timeline" class="text-white me-4 fs-4"><i class="bi bi-arrow-left"></i></a>
          <div>
              <h5 class="mb-0 fw-bold"><?= htmlspecialchars($this->view->dados_usuario['nome']) ?></h5>
              <small class="text-muted"><?= $this->view->total_tweets ?> posts</small>
          </div>
      </div>

      <div class="bg-secondary position-relative" style="height: 200px; background-color: #333639 !important;">
          <?php if(!empty($this->view->dados_usuario['imagem_capa'])) : ?>
            <img src="/uploads/<?= $this->view->dados_usuario['imagem_capa'] ?>" class="w-100 h-100 object-fit-cover">
          <?php endif; ?>
          
      </div>

      <div class="px-3 position-relative">
          <div class="bg-danger rounded-circle d-flex align-items-center justify-content-center border border-5 border-black position-absolute" 
               style="width: 140px; height: 140px; top: -70px; left: 15px; overflow: hidden; background-color: #d0312d !important;">
              
              <?php if(!empty($this->view->dados_usuario['imagem_perfil'])) : ?>
                  <img src="/uploads/<?= $this->view->dados_usuario['imagem_perfil'] ?>" class="w-100 h-100 object-fit-cover">
              <?php else : ?>
                  <span style="font-size: 4rem; font-weight: bold;"><?= strtoupper(substr($this->view->dados_usuario['nome'], 0, 1)) ?></span>
              <?php endif; ?>
          </div>

          <div class="d-flex justify-content-end py-3">
              <button class="btn btn-outline-light rounded-pill fw-bold px-4" data-bs-toggle="modal" data-bs-target="#modalEditar">Edit profile</button>
          </div>

          <div class="mt-4 pt-2">
              <h4 class="mb-0 fw-bold"><?= htmlspecialchars($this->view->dados_usuario['nome']) ?></h4>
              <p class="text-muted">@<?= htmlspecialchars($this->view->dados_usuario['nome']) ?></p>
              
              <?php if(!empty($this->view->dados_usuario['biografia'])) : ?>
                <p class="mb-2"><?= nl2br(htmlspecialchars($this->view->dados_usuario['biografia'])) ?></p>
              <?php endif; ?>

              <div class="text-muted mb-3 small d-flex gap-3">
                  <span><i class="bi bi-calendar3 me-2"></i>Joined July 2025</span>
                  <?php if(!empty($this->view->dados_usuario['localizacao'])) : ?>
                    <span><i class="bi bi-geo-alt me-1"></i> <?= htmlspecialchars($this->view->dados_usuario['localizacao']) ?></span>
                  <?php endif; ?>
              </div>

              <div class="d-flex gap-3 small">
                  <div><span class="fw-bold"><?= $this->view->total_seguindo ?></span> <span class="text-muted">Following</span></div>
                  <div><span class="fw-bold"><?= $this->view->total_seguidores ?></span> <span class="text-muted">Followers</span></div>
              </div>
          </div>
      </div>

      <div class="d-flex border-bottom border-secondary mt-4">
          <div class="flex-grow-1 text-center py-3 fw-bold border-bottom border-4 border-primary">Posts</div>
          <div class="flex-grow-1 text-center py-3 text-muted fw-bold">Replies</div>
          <div class="flex-grow-1 text-center py-3 text-muted fw-bold">Media</div>
      </div>

      <div class="posts">
        <?php foreach($this->view->tweets as $id_tweet => $tweet): ?>
          <div class="px-3 pt-3 pb-3 border-bottom border-secondary">
            <div class="d-flex align-items-center mb-1">
              <span class="fw-bold me-1 text-white"><?= htmlspecialchars($tweet['nome']) ?></span>
              <span class="text-muted small">@<?= htmlspecialchars($tweet['nome']) ?> · <?= htmlspecialchars($tweet['data']) ?></span>
            </div>
            <div class="mb-2 text-white"><?= nl2br(htmlspecialchars($tweet['tweet'])) ?></div>

            <?php if($tweet['id_usuario'] == $_SESSION['id']) : ?>
                <form method="post" action="/remover" onsubmit="return confirm('Excluir tweet?');">
                  <input type="hidden" name="tweet_id" value="<?= $tweet['id'] ?>">
                  <button type="submit" class="btn btn-outline-danger btn-sm border-0"><i class="bi bi-trash"></i></button>
                </form>
            <?php endif; ?>
          </div>
        <?php endforeach ?>
      </div>
    </div>

    <div class="col-3 py-3 px-4 border-start border-secondary order-3">
      <div class="p-3 border-bottom border-secondary">
        <form method="GET" action="/timeline" class="d-flex">
          <input type="text" name="pesquisarPor" class="form-control me-2 bg-black text-white border border-secondary  ps-3" placeholder="Quem você está procurando">
          <button type="submit" class="btn btn-outline-light btn-sm ">Procurar</button>
        </form>
      </div>
      
      <div class="mt-4 ">
          
          <?php if (!empty($this->view->usuarios)) : ?>
            <?php foreach($this->view->usuarios as $usuario): ?>
                <div class="d-flex justify-content-between align-items-center py-2">
                    <div>
                        <div class="fw-bold small"><?= htmlspecialchars($usuario['nome']) ?></div>
                        <div class="text-muted small">@<?= htmlspecialchars($usuario['nome']) ?></div>
                    </div>
                    <a href="/acao?acao=<?= $usuario['seguindo_sn'] == 0 ? 'seguir' : 'deixar_de_seguir' ?>&id_usuario=<?= $usuario['id'] ?>" class="btn btn-light btn-sm rounded-pill fw-bold">Follow</a>
                </div>
            <?php endforeach; ?>
          <?php endif; ?>
      </div>
    </div>

  </div>
</div>

<div class="modal fade" id="modalEditar" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-lg">
    <div class="modal-content bg-black text-white border border-secondary rounded-4">
      <form action="/salvar_perfil" method="post" enctype="multipart/form-data">
          <div class="modal-header border-0">
            <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            <h5 class="modal-title fw-bold ms-3">Edit profile</h5>
            <button type="submit" class="btn btn-light rounded-pill fw-bold ms-auto px-4">Save</button>
          </div>
          
          <div class="modal-body p-0">
             <div class="position-relative bg-secondary" style="height: 200px;">
                 <?php if(!empty($this->view->dados_usuario['imagem_capa'])) : ?>
                    <img src="/uploads/<?= $this->view->dados_usuario['imagem_capa'] ?>" class="w-100 h-100 object-fit-cover opacity-50">
                 <?php endif; ?>
                 <label for="inputCapa" class="position-absolute top-50 start-50 translate-middle btn btn-edit-photo rounded-circle p-3">
                     <i class="bi bi-camera fs-4"></i>
                 </label>
                 <input type="file" name="imagem_capa" id="inputCapa" class="d-none">
             </div>

             <div class="px-3" style="margin-top: -60px;">
                 <div class="position-relative d-inline-block">
                     <div class="rounded-circle d-flex align-items-center justify-content-center border border-4 border-black bg-danger" 
                          style="width: 120px; height: 120px; overflow: hidden;">
                         <?php if(!empty($this->view->dados_usuario['imagem_perfil'])) : ?>
                             <img src="/uploads/<?= $this->view->dados_usuario['imagem_perfil'] ?>" class="w-100 h-100 object-fit-cover opacity-50">
                         <?php else : ?>
                             <span style="font-size: 3rem; font-weight: bold;"><?= strtoupper(substr($this->view->dados_usuario['nome'], 0, 1)) ?></span>
                         <?php endif; ?>
                     </div>
                     <label for="inputPerfil" class="position-absolute top-50 start-50 translate-middle btn btn-edit-photo rounded-circle p-2">
                         <i class="bi bi-camera fs-5"></i>
                     </label>
                     <input type="file" name="imagem_perfil" id="inputPerfil" class="d-none">
                 </div>
             </div>

             <div class="p-3 mt-3">
                 <div class="form-floating mb-3">
                   <input type="text" class="form-control bg-black text-white border-secondary" name="nome" value="<?= htmlspecialchars($this->view->dados_usuario['nome']) ?>" required>
                   <label class="text-muted">Name</label>
                 </div>
                 <div class="form-floating mb-3">
                   <textarea class="form-control bg-black text-white border-secondary" name="biografia">
                      <?= htmlspecialchars($this->view->dados_usuario['biografia'] ?? '') ?>
                    </textarea>
                   <label class="text-muted">Bio</label>
                 </div>
                 <div class="form-floating mb-3">
                   <input type="text" class="form-control bg-black text-white border-secondary" name="localizacao" 
                    value="<?= htmlspecialchars($this->view->dados_usuario['localizacao'] ?? '') ?>">
                   <label class="text-muted">Location</label>
                 </div>
                 <div class="form-floating mb-4">
                    <input type="text" class="form-control bg-black text-white border-secondary" name="site" 
                    value="<?= htmlspecialchars($this->view->dados_usuario['site'] ?? '') ?>">
                   <label class="text-muted">Website</label>
                 </div>
             </div>
          </div>
      </form>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>